<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.sgost" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Tus configuraciones actuales -->
        <item name="android:windowBackground">@color/color_splash</item>

        <!-- Opcional: Sincroniza la barra de estado -->
        <item name="android:statusBarColor">@color/color_splash</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>